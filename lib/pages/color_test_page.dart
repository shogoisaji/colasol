import 'package:flutter/material.dart';

class ColorTestPagge extends StatelessWidget {
  const ColorTestPagge({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
          child: Column(
        children: [
          Text('Test Page'),
        ],
      )),
    );
  }
}
