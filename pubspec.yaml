name: colasol
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  riverpod_annotation:

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  riverpod_generator:

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
